# The skill needs NOTHING here. `resolve`, `analyze` and the HTML report all run on the
# Python 3.12 standard library alone, and the report is printed to PDF by the browser.
#
# The single optional path is the MCP server (server.py), which swaps shell commands for
# typed tools. Its dependency is declared in that file's PEP 723 header, so `uv run` handles it:
#   mcp>=2,<3
