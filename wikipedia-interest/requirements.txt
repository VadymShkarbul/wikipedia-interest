# The skill's core needs NOTHING here: `resolve` and `analyze` and the default HTML report
# run on the Python 3.12 standard library alone. These are the two optional paths.

# Typed MCP tools instead of shell commands (scripts/../server.py):
#   mcp>=2,<3

# PDF output instead of the default self-contained HTML (`report --format pdf`):
#   matplotlib>=3.7
